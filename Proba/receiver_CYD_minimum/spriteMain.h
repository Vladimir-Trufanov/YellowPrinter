/** YellowPrinter-Esp32-Arduino                            *** spriteMain.h ***
 * 
 * Представить на экране дисплея поступившие строки и уложенные в спрайт
 * 
 * v1.0.0, 08.07.2026                                 Автор:      Труфанов В.Е.
 * Copyright © 2026 tve                               Дата создания: 12.04.2024
**/

#pragma once

#include <Arduino.h>
#include <TFT_eSPI.h>

char* IntToChar(uint16_t numbIn); 

class TSprite_Main 
{
  public:

  TSprite_Main(int iQueueSize=4); 
  void init(); 
  void sayLine(int16_t x, int16_t y, char* line); 
  void chsay(); 
  void clrScr(bool color); 

  private:

  TFT_eSPI tft = TFT_eSPI();
  TFT_eSprite stext3 = TFT_eSprite(&tft); 

  uint16_t i=0;
  char line[128];  // строка спрайта
  char fill[128];  // заполнитель строки


  /*
  // Определяем счетчик прерываний от таймера и общее их количество до
  // события отправки одного из четырех сообщений = чуть быстрее четверти секунды 
  unsigned int cntr = 0;                      // счетчик тиков таймера
  volatile unsigned int timerToggle = 21000;  // 62499 -> 1 секунда;
  volatile bool OneSecondFlag = false;        // истечение 1 сек для запуска трассировок
  volatile bool Motor1_Flag = false;          // истечение 1 четверти сек для отправки данных по мотору
  volatile bool Vcc2_Flag = false;            // истечение 2 четверти сек для отправки данных по напряжению батареи
  volatile bool Act3_Flag = false;            // истечение 3 четверти сек для подтверждения крайней команды
  volatile byte quatr = 0;                    // счетчик четвертей секунды
  */
};


  TSprite_Main::TSprite_Main(int iQueueSize) 
  {
    // Чистим заполнитель
    memset(fill,32,127); 
    fill[127]='\0';
    //
    int t=iQueueSize;
  };

  void TSprite_Main::clrScr(bool color) 
  {
    bool c=color;
  }

  void TSprite_Main::init() 
  {
    // Задаем среднюю глубину цвета и создаем спрайт
    // заданного размера в пикселах в оперативной памяти
    stext3.setColorDepth(1);
    //stext3.createSprite(200, 110);
    stext3.createSprite(288, 224);
    //stext3.createSprite(244, 208);
    //stext3.createSprite(280, 160);
    // Заполняем буфер памяти, выделенный под спрайт, заданным цветом
    stext3.fillSprite(TFT_BLACK);
    // Отключаем перенос текста и по горизонтали и по вертикали 
    stext3.setTextWrap(false, false);
    // Определяем цвет текста с прозрачным фоном  
    stext3.setTextColor(TFT_WHITE,TFT_BLACK,true); 
    // Загружаем шрифт в память спрайта
    stext3.loadFont("HuaweiSans16");     // загрузка в память шрифта
    //stext3.loadFont("nasalization48");     
  }
  
  void TSprite_Main::sayLine(int16_t x, int16_t y, char* line) 
  {
    // Устанавливаем позицию курсора для вывода текста в заданной позиции спрайта
    stext3.setCursor(x,y);
    stext3.print(fill);
    stext3.setCursor(x,y);
    stext3.print(line); 
  }

  // Так как предполагается вывод всего 2 строк, то 
  // вначале выводим прежнее значение в нижней части
  void TSprite_Main::chsay() 
  {
    char chi[6]="Чи";
    char exclam[2]="!";

    memset(line,'\0',128); 
    strcat(line,chi); 
    strcat(line,IntToChar(i)); 
    strcat(line,exclam); 
    //sayLine(10,55,line);
    sayLine(10,25,line);

    // Выше складываем новое значение
    i++;
    memset(line,'\0',128); 
    strcat(line,chi); 
    strcat(line,IntToChar(i)); 
    strcat(line,exclam); 
    sayLine(10,10,line);
    
    // Выталкиваем спрайт на дисплей с заданной позиции
    //stext3.pushSprite(10,60);
    stext3.pushSprite(16,16);
  }

  // ****************************************************************************
  // *            Преобразовать беззнаковое  целое в строку символов            *
  // ****************************************************************************
  char charNumby[6]; // char[5]+'\0'
  char* IntToChar(uint16_t numbIn) 
  {
    uint16_t numby=numbIn;
    memset(charNumby,'\0',6); 
    if (numby>65534) numby=0;
    String(numby).toCharArray(charNumby,6);
    return charNumby;
  }

  /*
У CYD (это DIY-проект с экраном на ESP32 и библиотекой TFT_eSPI) нет жёсткого лимита на ширину отдельного спрайта. Я проверила обсуждения на GitHub: люди используют экраны 320×240, и в коде нет проверки, которая бы отсекала изображения шире экрана. То есть если картинка влезет в память дисплея — она отобразится целиком. 

На практике важнее другое: если сделать слишком широкие или «толстые» спрайты (например, с большим отступом от края), это съест память и замедлит отрисовку, особенно если у вас много таких изображений. Часто советуют делать ширину кратной 16 пикселям — так проще работать со спрайт-листами и не терять производительность. 

Мой совет: перед тем как рисовать большой спрайт, запустите тестовую программу. Загрузите картинку и посмотрите, как она отрисовывается — так вы сразу поймёте, вписывается ли она в ваш бюджет памяти и не тормозит ли вывод.

Я
Какие факторы влияют на выбор ширины спрайта?
Алиса
Для CYD (ESP32 + TFT_eSPI) выбор ширины спрайта зависит не от «жёсткого лимита», а от баланса между памятью, скоростью отрисовки и удобством работы с графикой.

Ключевые факторы
Объём доступной RAM. Спрайт в памяти хранится как массив пикселей: для 16‑битного цвета (RGB565) это ширина × высота × 2 байта. Если спрайт слишком широкий, он может не поместиться в свободную кучу (heap) или сильно её фрагментировать. Для ESP32 это критично: даже если формально хватает памяти, при активной отрисовке и аллокациях программа может падать с ESP_ERR_NO_MEM.
Размер экрана и видимая область. Часто нет смысла делать спрайт шире экрана (например, 320 px): всё, что выходит за границы, не видно, а память и время на отрисовку тратятся. Исключение — скролл, параллакс, анимация «выезда» объекта: тогда спрайт делают с запасом.
Формат цвета и битность. RGB565 (2 байта/пиксель) — самый частый вариант. Если перейти на 1‑битный монохром или палитру, можно сильно экономить память, но это меняет качество и усложняет подготовку графики.
Производительность отрисовки. Копирование широких строк пикселей (DMA или простой memcpy) линейно зависит от ширины. Чем шире спрайт, тем дольше он рисуется. В играх это напрямую влияет на FPS.
Выравнивание и оптимизация библиотеки TFT_eSPI. Библиотека лучше работает, когда ширина кратна 16 или 32 пикселям: так проще использовать блочные операции и избегать «хвостов» по краям строк. Это не строго обязательно, но заметно ускоряет вывод.
Структура спрайт‑листа (sprite sheet). Если вы храните много кадров анимации в одном большом изображении, ширина листа должна быть кратна ширине кадра и учитывать количество кадров в строке. Это упрощает адресацию и обрезку отдельных кадров.
Источник графики и подготовка. Растровые картинки из редактора могут иметь «лишние» поля, прозрачные зоны или некратные размеры. Их лучше обрезать и подгонять под целевые размеры ещё на этапе подготовки, чтобы не тратить память и CPU в рантайме.
Динамическое создание vs загрузка из PROGMEM/SPIFFS. Если спрайт создаётся в RAM «на лету», лимит ещё жёстче. Если он лежит в PROGMEM (как статический массив) и копируется в буфер по частям, можно работать с более широкими изображениями, но усложняется логика отрисовки.
Конкуренция за ресурсы с другими задачами. ESP32 часто делает не только графику, но и Wi‑Fi, логирование, обработку датчиков. Большой спрайт может временно «съесть» почти всю свободную память и вызвать проблемы у других подсистем.
Практические рекомендации для Arduino/ESP32
Для типичного экрана 320×240 держите ширину спрайта не больше 320, а лучше — в пределах 128–256 px для одиночных объектов.
Для спрайт‑листов подбирайте ширину листа так, чтобы в строку помещалось целое число кадров (например, 4 кадра по 64 px → ширина листа 256 px).
Всегда проверяйте потребление памяти: используйте heap_caps_get_free_size(MALLOC_CAP_8BIT) до и после создания спрайта.
Если видите тормоза — уменьшайте ширину и высоту, переходите на меньшую битность или рисуйте спрайт частями.
*/


  // *********************************************************** spriteMain.h ***
